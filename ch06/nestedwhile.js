let dan=2;
let num=1;

while (dan<=9) {
    num = 1;
    console. log(`${dan}단`)
    while (num<=9) {

        console.log(`${dan} x ${num}=${dan*num}`)
        num++;
    }
    dan++;
    console.log(`\n`)
}
